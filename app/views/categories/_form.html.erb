<%= form_with(model: cate) do |form| %>
  <% if cate.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(cate.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
        <% cate.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
