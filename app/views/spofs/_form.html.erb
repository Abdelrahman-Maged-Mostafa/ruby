<%= form_with(model: spof) do |form| %>
  <% if spof.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(spof.errors.count, "error") %> prohibited this spof from being saved:</h2>

      <ul>
        <% spof.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :label, style: "display: block" %>
    <%= form.textarea :label %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
